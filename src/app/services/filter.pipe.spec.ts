/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InputFilter } from './query.service';

describe('FilterPipe', () => {
  it('create an instance', () => {
    let pipe = new InputFilter();
    expect(pipe).toBeTruthy();
  });
});
